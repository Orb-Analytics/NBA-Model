name: Update Training Set from Google Sheets

on:
  schedule:
    - cron: '30 17 * * *'  # Runs daily at 10:30 AM PT (17:30 UTC)
  workflow_dispatch:        # Allow manual runs from GitHub Actions tab

jobs:
  update-training-set:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repo
      - name: Checkout repo
        uses: actions/checkout@v4

      # Step 2: Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          pip install pandas requests python-dateutil pytz \
            google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client

      # Step 4: Recreate your service account JSON from GitHub secret
      - name: Create service account file
        run: |
          echo '${{ secrets.SERVICE_ACCOUNT_JSON }}' > service_account.json

      # Step 5: Run complete daily update pipeline
      - name: Run complete NBA data pipeline
        run: |
          python src/daily_update.py

      # Step 6: Commit and push updates
      - name: Commit and push updates
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Add all updated data files
          git add "data/NBA Training Set 25-26.csv" || true
          git add data/novig-odds/*.csv || true
          git add data/yesterdays_scores/*.csv || true
          git add data/raw/*.csv || true
          git add data/unmatched_games.csv || true

          git commit -m "🗓️ Complete NBA data pipeline update ($(date -u +"%Y-%m-%d %H:%M UTC"))" || echo "No changes to commit"
          git push
